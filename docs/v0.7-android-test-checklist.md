# V0.7 Android Test Checklist

日期：2026-06-02

## 范围

- 首页删除「待处理」「申请邀请码」，最新动态展示全部设备当前状态并按设备更新时间排序。
- 首页「查看列表」进入设备列表，并默认展示「在我手上」。
- 设备列表删除「借入待还」Tab，借入待还设备合并到「在我手上」。
- 借入待还设备详情不可编辑名称，不可转让 owner。
- Owner 用户列表 / 邀请码 Tab 可点击切换，用户列表删除「查看邀请码」按钮，管理员入口按系统按钮规范展示。
- 后端设备响应增加 `createdAt` / `updatedAt`，Android remote 数据使用 `updatedAt` 排序与展示。

## 自动验证

- Android：`assembleDebug` 通过。
- Android：`testDebugUnitTest` 通过。
- Android：`lintDebug` 通过。
- Backend：`test` 通过。

## 模拟器验证

- 截图：`docs/test-artifacts/v0.7/home-returned.png`
  - 首页无「待处理」和「申请邀请码」。
  - 首页最新动态展示设备名、状态、当前持有人、录入/更新时间。
- 截图：`docs/test-artifacts/v0.7/devices-on-hand-from-home.png`
  - 从首页「查看列表」进入设备列表。
  - Tab 为「全部 / 在我手上 / 已借出」。
  - 「借入待还」设备在「在我手上」列表内。
- 截图：`docs/test-artifacts/v0.7/borrowed-detail-permission.png`
  - 借入待还设备详情展示「无编辑权限」。
  - 转让 owner 显示无权限说明。
- 截图：`docs/test-artifacts/v0.7/owner-users.png`
  - 用户列表 / 邀请码 Tab 在页面上方。
  - 无「查看邀请码」按钮。
  - 用户新增、编辑、停用/删除入口可见。
- 截图：`docs/test-artifacts/v0.7/owner-invites.png`
  - 从用户列表 Tab 切换到邀请码页。
  - 生成邀请码入口可见。

## 结论

V0.7 本地自测通过，待用户人工验收。
