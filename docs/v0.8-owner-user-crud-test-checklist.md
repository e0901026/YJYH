# V0.8 Owner User CRUD Test Checklist

日期：2026-06-02

## 范围

- Owner 用户管理新增真实后端接口：
  - `POST /api/owner/users`
  - `PUT /api/owner/users/{id}`
  - `DELETE /api/owner/users/{id}`，语义为停用账号。
- 停用账号后禁止登录。
- Android Owner 用户页新增用户表单、编辑回填、保存修改、停用/删除入口。
- Android Owner 邀请码页生成按钮接入真实后端 `POST /api/owner/invite-codes`。

## 自动验证

- Backend：`./gradlew test` 通过。
- Android：`./gradlew :app:assembleDebug :app:testDebugUnitTest` 通过。
- Android：`./gradlew :app:lintDebug` 通过。

## 本地 API 验证

- 登录 Owner 获取 token：成功。
- 新增用户：成功返回 `enabled=true`。
- 编辑用户：成功更新名称和角色。
- 停用用户：成功返回 `enabled=false`。
- 生成邀请码：成功返回 `OWNER-` 前缀邀请码。

## 模拟器验证

- 截图：`docs/test-artifacts/v0.8/owner-user-crud-form.png`
  - 用户管理页展示新增/编辑表单。
  - 表单包含工号、名称、密码、普通用户/Owner 角色切换。
  - 用户列表展示角色和启用状态。
- 截图：`docs/test-artifacts/v0.8/owner-user-created.png`
  - 模拟器进入真实 Owner 用户管理页面并显示新增用户表单。

## 结论

V0.8 Owner 用户管理真实 CRUD 和邀请码生成通过本地自测，待用户人工验收。
